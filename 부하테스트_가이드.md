# FreeSWITCH → XMS146 부하테스트 가이드

10채널 송신 부하 검증. 두 가지 방법:
- fs_load_xms.sh: fs_cli 직접 발송 (엔진 순수 성능)
- app_load_xms.py: faxapp 경유 (실제 서비스 경로)

## 사전 준비

### 1. 발송할 팩스 파일 (FreeSWITCH VM=141에)
```bash
# Pillow 설치
pip install Pillow --break-system-packages
# 10페이지 팩스 생성 (make_load_fax.py 사용)
python3 make_load_fax.py --pages 10 --out /tmp/load10.tif
file /tmp/load10.tif   # group4 확인
```

### 2. 게이트웨이 확인
```bash
fs_cli -x "sofia status gateway xms146"
# Status UP 확인
```

### 3. 146 수신엔진 실행중 확인
```bash
# 146에서 이미 실행중이어야:
# XMS_HOST=192.168.219.146 FAX_INBOUND=1 FAX_WEB_PORT=8091 \
#   FAX_APP_RECV_WEBHOOK=http://192.168.219.141:8100/api/receive/webhook \
#   python3 faxserver_xms.py
```

## 방법 A: fs_cli 직접 부하 (엔진 성능)

```bash
# FreeSWITCH VM(141)에서
bash fs_load_xms.sh 10 /tmp/load10.tif
#              ↑채널  ↑팩스파일

# 단계적으로:
bash fs_load_xms.sh 3 /tmp/load10.tif    # 3채널 먼저
bash fs_load_xms.sh 5 /tmp/load10.tif    # 5채널
bash fs_load_xms.sh 10 /tmp/load10.tif   # 10채널 풀로드
```

관찰:
- 스크립트 출력: 발송 성공/실패
- 146 수신엔진 로그: [수신완료] bit_rate=33600 (10건 떠야)
- fs_cli -x "show channels count": 동시 채널 수

## 방법 B: faxapp 경유 부하 (실제 서비스 경로)

```bash
# faxapp VM에서
python3 app_load_xms.py --count 10 --pages 10 --password '비번'
```

이건 faxapp API로 발송 → 전체 경로 검증:
faxapp → FreeSWITCH엔진 → 게이트웨이 → 146

## 판정 기준

| 지표 | 정상 |
|---|---|
| 발송 성공률 | 10/10 (100%) |
| 146 수신 [수신완료] | 10건 |
| bit_rate | 33600 (V.34 유지) |
| 동시 채널 | 최대 10 (XMS 라이선스) |

## 주의: XMS 10채널 한계

XMS가 10채널이므로:
- 10건 동시 = 딱 맞음
- 11건 이상 = 초과분은 대기 or 503
- 송신(141→146)만 채널 쓰는 게 아니라
  146 수신도 채널 사용 → 실제 동시 처리량 확인 필요

## 트러블슈팅

**발송은 되는데 146 수신 안 뜸:**
- 146 수신엔진 로그 확인 (incoming 이벤트 오나)
- 146 XMS 라우팅 (.* → app)

**bit_rate가 14400 (V.34 안 됨):**
- FreeSWITCH가 T.38로 협상한 것
- fs_load_xms.sh는 fax_enable_t38=false로 이미 방지
- 게이트웨이/프로파일에서 T.38 강제 안 하는지 확인

**503 / 채널 부족:**
- XMS 10채널 초과. 좀비 콜 확인:
  curl -sk -u admin:admin "https://192.168.219.146:444/default/calls?appid=app"
- 좀비 있으면 xms_cleanup.py로 정리
